import React from 'react';

// This component is currently not in use and returns null
// as per user feedback to remove profile/payment confirmation page.
const ProfilePage: React.FC = () => {
  return null;
};

export default ProfilePage;